{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run FTS",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Client/build/FTSClient",
            "preLaunchTask": "Build and Run FTS",
            "cwd": "${workspaceFolder}/Client",
            "MIMode": "gdb",
            "externalConsole": false
        }
    ]
}